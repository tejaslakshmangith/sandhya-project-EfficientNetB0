# Dataset Structure

Place your images in the following structure:

```
dataset/
├── train/
│   ├── safe/       ← safe mining condition images
│   └── unsafe/     ← unsafe mining condition images
└── val/
    ├── safe/
    └── unsafe/
```

Minimum recommended: 100 images per class per split.
